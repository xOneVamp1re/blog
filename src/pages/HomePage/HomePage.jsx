import { ArticlesList } from '@widgets/ArticlesList'

export const HomePage = () => {
  return (
    <div
      style={{
        textAlign: 'center',
        marginTop: '100px',
      }}>
      <ArticlesList />
    </div>
  )
}
